{
  "name": "expense-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "A personal expense tracker with real accounts, strict per-user data isolation, and a retry-safe payment write path.",
  "main": "server.js",
  "scripts": {
    "start": "node --no-warnings server.js",
    "test": "node --no-warnings --test"
  },
  "engines": {
    "node": ">=22.5.0"
  }
}
